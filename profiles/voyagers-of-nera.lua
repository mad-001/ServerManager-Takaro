-- Voyagers of Nera profile for ServerManager-Takaro
-- Engine: Unreal Engine 5.4 (project "BoatGame"). Online: EOS (RedpointEOS).
-- Player identity is the EOS ProductUserId, read from PlayerState.UniqueId by autodetect;
-- `platform` namespaces any synthesized fallback platformId (never used once the EOS id
-- is read). chat/death still auto-discover at runtime.
return {
    name = "Voyagers of Nera",
    platform = "voyagersofnera",
}
